# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Google AI
google-generativeai==0.8.3

# Utilities
python-dotenv==1.0.0
httpx==0.26.0

# Background Tasks
# Option 1: Celery + Redis (for paid tier with separate worker)
celery[redis]==5.3.6
redis==5.0.1

# Option 2: APScheduler (for free tier, runs in same process)
apscheduler==3.10.4

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
